export class Starship {
    id: Number;
    name: String;
    registry: String;

    constructor(id: Number, name: String, registry: String) {
        this.id = id;
        this.name = name;
        this.registry = registry;
    }
}
